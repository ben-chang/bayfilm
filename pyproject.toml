[project]
name = "bayfilm"
version = "0.1.0"
description = "Showtime aggregator for Bay Area independent movie theaters"
requires-python = ">=3.11"
dependencies = [
    "requests>=2.31",
    "beautifulsoup4>=4.12",
]

[tool.setuptools]
packages = ["scraper", "scraper.theaters"]
